import { createStore } from 'vuex'
import scrapeApi from './scrapeApi'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    scrapeApi: scrapeApi,
  }
})
